# Final Checklist

Use this immediately before delivery.

- Output images are grouped by task/theme in one output folder.
- Previous output groups were not overwritten.
- Expected files exist for requested resources:
  - `channel_1041x225.png`
  - `category_banner_1041x217.png`
  - `push_1035x390.png`
  - `feed_503x645.png`
  - `popup_885x990.png`
  - `splash_1125x1956.png`
  - `mini_program_share_600x480.png`
- Output dimensions match the renderer report.
- Rounded templates keep transparent corners when applicable.
- The title/subtitle generated by image gen are visible, correctly aligned for the resource orientation, and not clipped. If the one allowed title-Y retry still leaves the title outside its allowed range, the remaining direction and distance are recorded as a delivery warning.
- The image gen input image has no CTA button baked into it.
- `material.json` keeps `title` and `subtitle` empty; CTA copy comes from `material.json` or parsed request text.
- For `feed` and `popup`, CTA background stays distinguishable from its local background.
- For `feed` and `popup`, CTA text remains readable against the selected button background.
- The rendered CTA is visible and not clipped where the template displays a button.
- Render QA is `PASS`; every remaining Pre-render QA deviation is recorded as an observation or attention note.
- If `categoryBanner` reused the `channel` background, delivery notes say: `categoryBanner 由 channel 横图共享生成。`

Do not require report files in default fast mode. Only check or deliver `qa-report.md` / `qa-report.json` when the user explicitly asks to generate a report.
